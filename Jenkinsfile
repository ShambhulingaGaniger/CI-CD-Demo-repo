pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Fetching code from GitHub'
            }
        }

        stage('Build') {
            steps {
                echo 'Build step running'
            }
        }

    }
}
